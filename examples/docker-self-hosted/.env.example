# Required: AssemblyAI API key for speech-to-text and text-to-speech
ASSEMBLYAI_API_KEY=your-key-here

# Optional: Shared secret for authenticating WebSocket connections
# AUTH_TOKEN=some-random-secret

# Optional: Port to listen on (default: 3000)
# PORT=3000
