---
title: aai
description: A voice agent is a directory of TypeScript files. Talk to it in your browser, put it on a phone number, ship it with one command.
template: splash
hero:
  tagline: A voice agent is a directory of TypeScript files. Talk to it in your browser, put it on a phone number, ship it with one command.
  actions:
    - text: Quickstart
      link: /agent/start/quickstart/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/alexkroman/agent
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Install

```sh
npm i -g @alexkroman1/aai-cli
aai init my-agent
cd my-agent
aai dev
```

That prints a URL you can open and talk to. Requires Node.js 24, 25, or 26
— the range a scaffolded project declares — and an `ASSEMBLYAI_API_KEY`: one
key covers listening, thinking, and speaking.

## A whole agent

```ts
// agent.ts
import { agent } from "@alexkroman1/aai";

export default agent({
  name: "Weather Line",
  greeting: "Weather line — which city are you asking about?",
  voice: "michael",
});
```

```md
<!-- system-prompt.md -->
You help callers plan around the weather.
Answer in one or two sentences — this is a phone call, not a paragraph.
```

```ts
// tools/get_weather.ts
// The model calls this `get_weather`.
import { tool } from "@alexkroman1/aai";
import { z } from "zod";

export default tool({
  description: "Get current weather for a city",
  inputSchema: z.object({ city: z.string().describe("City name") }),
  execute: async ({ city }) => {
    const where = encodeURIComponent(city);
    const res = await fetch(`https://wttr.in/${where}?format=j1`);
    return await res.json();
  },
});
```

There is no registration step. A file in `tools/` is a tool because it is in
`tools/`, and `system-prompt.md` is the system prompt because it exists.

<CardGrid>
  <Card title="Start here" icon="rocket">
    [Quickstart](/agent/start/quickstart/) builds and publishes a working
    agent in about five minutes.
  </Card>
  <Card title="What a project looks like" icon="open-book">
    [How it works](/agent/start/how-it-works/) is the one page that explains why the
    project looks the way it does.
  </Card>
  <Card title="Give it abilities" icon="setting">
    [Tools](/agent/build/tools/) are ordinary async functions. The model decides when
    to call them.
  </Card>
  <Card title="Every option" icon="document">
    The [SDK reference](/agent/reference/) is generated from the source, and
    documents every field, provider, and helper.
  </Card>
</CardGrid>
