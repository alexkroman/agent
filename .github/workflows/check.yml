name: Check

on:
  push:
    branches: [changeset-release/main]
  pull_request:
    branches: [main]
  workflow_dispatch: {}

concurrency:
  group: check-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 26
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      # NO .tsbuildinfo cache here. This job runs `turbo run build`, and the
      # build configs set `incremental: false` — only `typecheck` writes
      # `.tsbuildinfo/`, so the step that used to sit here restored into a job
      # that never wrote the directory, and the job that does write it
      # (lint-typecheck-and-checks) never saved. tsc therefore ran cold on
      # every turbo cache miss. The cache now lives beside its producer.
      - name: Cache Turborepo
        uses: actions/cache@v6
        with:
          path: .turbo
          key: turbo-${{ runner.os }}-${{ github.sha }}
          restore-keys: turbo-${{ runner.os }}-
      - run: pnpm exec turbo run build
      - uses: actions/cache/save@v6
        with:
          path: |
            node_modules
            packages/*/node_modules
            packages/*/dist
            .turbo
          key: workspace-${{ runner.os }}-node${{ hashFiles('.node-version') }}-${{ github.sha }}

  lint-typecheck-and-checks:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 26
      - id: workspace-cache
        uses: actions/cache/restore@v6
        with:
          path: |
            node_modules
            packages/*/node_modules
            packages/*/dist
            .turbo
          key: workspace-${{ runner.os }}-node${{ hashFiles('.node-version') }}-${{ github.sha }}
      - run: pnpm install --frozen-lockfile
      # Beside the only task that writes it (`typecheck`; the build configs set
      # `incremental: false`). Keyed on the tsconfigs and the lockfile — what
      # invalidates a tsc program — with the run's SHA appended so each run
      # saves a fresh entry; a key that already exists is skipped on save, so a
      # SHA-less key would freeze the first run's buildinfo forever.
      - name: Cache TypeScript incremental build info
        uses: actions/cache@v6
        with:
          path: .tsbuildinfo/
          key: tsbuildinfo-typecheck-${{ runner.os }}-${{ hashFiles('packages/*/tsconfig.json', 'tsconfig.json', 'pnpm-lock.yaml') }}-${{ github.sha }}
          restore-keys: |
            tsbuildinfo-typecheck-${{ runner.os }}-${{ hashFiles('packages/*/tsconfig.json', 'tsconfig.json', 'pnpm-lock.yaml') }}-
            tsbuildinfo-typecheck-${{ runner.os }}-
      - run: pnpm exec turbo run lint typecheck
      - name: Check for changeset
        if: github.event_name == 'pull_request' && github.head_ref != 'changeset-release/main' && !contains(github.event.pull_request.labels.*.name, 'no-release')
        run: pnpm changeset status --since=origin/main
      - run: |
          pnpm exec turbo run check:publint check:attw docs
          pnpm run check:knip
          # `turbo run lint` above is per-PACKAGE, and scripts/ is not a
          # package — so until this line it was the one source directory in
          # the repo that no lint job touched.
          pnpm run lint:scripts
          pnpm run check:syncpack
          pnpm run check:sherif
          pnpm run check:markdown
          pnpm run check:publish-names
          pnpm run check:doc-examples
          # In scripts/check.sh since it was written, but missing here — the
          # same CI-never-invokes-check.sh gap the ratchets below document.
          # It type-checks the templates under the SCAFFOLD's tsconfig, which
          # is looser than the repo's, so `turbo run typecheck` passing above
          # says nothing about it.
          pnpm run check:template-types
      # The quality ratchets. They lived only in scripts/check.sh, which CI
      # never invokes, so they were enforced solely by the pre-push hook — and
      # `git push --no-verify` skipped them entirely. All are pure git/fs
      # checks needing no build, and check-escape-hatches degrades to a skip
      # when there is no origin/main to diff against. check:guest-toolchain
      # catches a lockfile that no longer matches the installed toolchain,
      # which would otherwise bake a different tree into every guest image
      # than the one this repo tested with.
      # check:konsistent enforces konsistent.json — the structural conventions
      # no per-file linter can see (a provider module missing one of its five
      # exports, a *-barrel.ts with a local declaration, a cross-boundary
      # import). It auto-emits ::error annotations here because GITHUB_ACTIONS
      # is set, so violations land inline on the PR diff with no extra flags.
      - run: |
          pnpm run check:hatches
          pnpm run check:file-length
          pnpm run check:test-assertions
          pnpm run check:claude-md
          pnpm run check:guest-toolchain
          pnpm run check:konsistent

  # Unit tests sharded by package — run in parallel
  test:
    needs: setup
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        package: [aai, aai-ui, aai-cli, aai-guest, aai-server, aai-studio-server, aai-studio-client, aai-templates]
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 26
      - id: workspace-cache
        uses: actions/cache/restore@v6
        with:
          path: |
            node_modules
            packages/*/node_modules
            packages/*/dist
            .turbo
          key: workspace-${{ runner.os }}-node${{ hashFiles('.node-version') }}-${{ github.sha }}
      - run: pnpm install --frozen-lockfile
      # Through turbo rather than pnpm directly, for the `^build` ordering.
      # Running the package script straight assumes the workspace cache restore
      # above put every dependency's `dist` back; when it does not (an evicted
      # or partial entry) the suite fails on a missing built artifact —
      # aai-server alone resolves two, `aai-ui/dist/default-client` and
      # `aai-guest/harness` — with an error that names a file nothing in the
      # package touches. Under turbo those builds are cache hits from the setup
      # job in the normal case and a real rebuild in the abnormal one.
      - run: pnpm exec turbo run test:coverage --filter "./packages/${{ matrix.package }}"

  integration:
    needs: setup
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # Windows excluded: tsdown/rolldown can't resolve .ts exports in aai dist
        os: [ubuntu-latest, macos-latest]
        node: [26]
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
      - id: workspace-cache
        uses: actions/cache/restore@v6
        with:
          path: |
            node_modules
            packages/*/node_modules
            packages/*/dist
            .turbo
          key: workspace-${{ runner.os }}-node${{ hashFiles('.node-version') }}-${{ github.sha }}
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec turbo run build
      # Postgres for the platform integration tests: the per-slug advisory lock
      # and the migration + its stores. Both assert PostgreSQL semantics that
      # their unit suites can only assert in prose (see
      # platform-lock.integration.test.ts).
      #
      # A step rather than a `services:` container, because those need Docker
      # and this job's matrix includes macos-latest, where it is unavailable.
      # The runner image ships the cluster; starting it is enough. The database
      # behaviour under test is OS-independent, so the ubuntu leg covers it and
      # the tests skip themselves on macOS, where AAI_TEST_PG_URL is unset.
      - name: Start Postgres (Linux)
        if: runner.os == 'Linux'
        run: |
          set -euo pipefail
          sudo systemctl start postgresql
          # Fail here rather than let the tests skip: a suite that silently
          # stops running is the failure mode these tests exist to avoid.
          pg_isready --host 127.0.0.1 --timeout 30
          sudo -u postgres psql -c "alter user postgres password 'postgres'"
          echo "AAI_TEST_PG_URL=postgres://postgres:postgres@127.0.0.1:5432/postgres" >> "$GITHUB_ENV"
          # And make a SKIP fail here. `pg_isready` above covers the server not
          # starting; it says nothing about the variable not arriving, and if it
          # did not arrive all five real-Postgres suites would skip themselves
          # and this job would pass — the exact "a check that exists without
          # running" failure their own comments say they must not become.
          echo "AAI_REQUIRE_PG=1" >> "$GITHUB_ENV"
      - run: pnpm exec turbo run check:integration

  e2e:
    needs: setup
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # Windows excluded: tsdown/rolldown can't resolve .ts exports in aai dist
        os: [ubuntu-latest, macos-latest]
        node: [26]
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
      - id: workspace-cache
        uses: actions/cache/restore@v6
        with:
          path: |
            node_modules
            packages/*/node_modules
            packages/*/dist
            .turbo
          key: workspace-${{ runner.os }}-node${{ hashFiles('.node-version') }}-${{ github.sha }}
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec turbo run build
      - uses: actions/cache@v6
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('packages/aai-cli/package.json') }}
      - run: pnpm --filter @alexkroman1/aai-cli exec playwright install --with-deps chromium
      # --concurrency=1: the e2e run republishes every publishable package from
      # the live workspace, truncating shared dist/ directories while it does.
      # Nothing may run beside it. Matches `pnpm test:e2e` and scripts/check.sh.
      - run: pnpm exec turbo run check:e2e --concurrency=1

  # Single gate job for branch protection — add only "ci" as a required check
  ci:
    runs-on: ubuntu-latest
    if: always()
    needs: [lint-typecheck-and-checks, test, integration, e2e]
    steps:
      - name: Check results
        run: |
          results="${{ needs.lint-typecheck-and-checks.result }} ${{ needs.test.result }} ${{ needs.integration.result }} ${{ needs.e2e.result }}"
          for r in $results; do
            if [ "$r" != "success" ] && [ "$r" != "skipped" ]; then
              echo "CI failed: one or more jobs did not succeed"
              exit 1
            fi
          done
          echo "All CI jobs passed"
