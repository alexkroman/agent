diff --git a/dist/init.js b/dist/init.js
index 0e75a52c967be1870badb3e7a064cf3fdddd19bc..35a180bac551806e85a11fdd8fd90b9fb4bbb963 100644
--- a/dist/init.js
+++ b/dist/init.js
@@ -1,49 +1,25 @@
 import { access, constants, mkdir, readFile, unlink, writeFile, } from 'node:fs/promises';
 import path from 'node:path';
-import { fileURLToPath } from 'node:url';
 /** Package name - hardcoded since it doesn't change */
 const PACKAGE_NAME = '@workflow/world-local';
+/** Package version - kept in step with package.json by the patch's own version pin. */
+const PACKAGE_VERSION = '4.2.4';
 let cachedPackageInfo = null;
 /**
- * Get the directory path for this module.
- * Works in ESM and falls back to a constant in CJS contexts (which shouldn't happen)
- */
-function getModuleDir() {
-    // In bundled CJS contexts, import.meta.url may be undefined or empty
-    if (typeof import.meta.url === 'string' && import.meta.url) {
-        return path.dirname(fileURLToPath(import.meta.url));
-    }
-    return null;
-}
-/**
- * Returns the package name and version from package.json.
- * The result is cached after the first read.
+ * Returns the package name and version.
  *
- * In bundled contexts where package.json cannot be read,
- * returns 'bundled' as the version.
+ * Read from a constant rather than from `package.json` on disk: the file is
+ * located relative to THIS MODULE, so any consumer that bundles world-local
+ * into a single artifact reads whatever `package.json` happens to sit beside
+ * that artifact, or none at all. The unresolvable case fell back to the string
+ * `'bundled'`, which `parseVersion` below rejects — so `initDataDir` threw
+ * `Invalid version string: "bundled"` and the world could not start at all.
  */
 export async function getPackageInfo() {
-    if (cachedPackageInfo) {
-        return cachedPackageInfo;
-    }
-    const moduleDir = getModuleDir();
-    if (moduleDir) {
-        try {
-            const content = await readFile(path.join(moduleDir, '../package.json'), 'utf-8');
-            cachedPackageInfo = JSON.parse(content);
-            return cachedPackageInfo;
-        }
-        catch {
-            // Fall through to bundled fallback
-        }
-    }
-    // Bundled context - package.json not accessible
-    cachedPackageInfo = {
-        name: PACKAGE_NAME,
-        version: 'bundled',
-    };
+    cachedPackageInfo ??= { name: PACKAGE_NAME, version: PACKAGE_VERSION };
     return cachedPackageInfo;
 }
+
 /** Filename for storing version information in the data directory */
 const VERSION_FILENAME = 'version.txt';
 /**
diff --git a/dist/streamer.js b/dist/streamer.js
index 9e4d9dd842a04428138e9c3b025dbcb2812790ae..ad4ff06a1c4d911bafc92b9c5d2298028e4c992e 100644
--- a/dist/streamer.js
+++ b/dist/streamer.js
@@ -272,6 +272,15 @@ export function createStreamer(basedir, tag) {
         async readFromStream(name, startIndex = 0) {
             const chunksDir = path.join(basedir, 'streams', 'chunks');
             let removeListeners = () => { };
+            // Detach unconditionally. `removeListeners` is `closeListener`, which
+            // DEFERS while `isReadingFromDisk` — so a cancel() landing mid-disk-read
+            // detached nothing, start() then threw on enqueue-after-close, and the
+            // `pendingClose` cleanup below was never reached. One leaked chunk:/close:
+            // pair per reader, and every later chunk copied into each leaked reader's
+            // buffer. Backported from upstream 5.0.0-beta's `teardown`.
+            let teardown = () => { };
+            // Set by cancel(), so start() stops enqueuing rather than throwing.
+            let readerCancelled = false;
             let pollInterval = null;
             return new ReadableStream({
                 async start(controller) {
@@ -331,6 +340,14 @@ export function createStreamer(basedir, tag) {
                         }
                     };
                     removeListeners = closeListener;
+                    teardown = () => {
+                        streamEmitter.off(`chunk:${name}`, chunkListener);
+                        streamEmitter.off(`close:${name}`, closeListener);
+                        if (pollInterval) {
+                            clearInterval(pollInterval);
+                            pollInterval = null;
+                        }
+                    };
                     // Set up listeners FIRST to avoid missing events
                     streamEmitter.on(`chunk:${name}`, chunkListener);
                     streamEmitter.on(`close:${name}`, closeListener);
@@ -361,6 +378,12 @@ export function createStreamer(basedir, tag) {
                         const chunkId = tag
                             ? rawChunkId.replace(`.${tag}`, '')
                             : rawChunkId;
+                        // The reader is gone: cancel() has already detached, so stop
+                        // rather than enqueue into a closed controller (which throws,
+                        // and used to abandon the cleanup below).
+                        if (readerCancelled) {
+                            return;
+                        }
                         // Skip if already delivered via event
                         if (deliveredChunkIds.has(chunkId)) {
                             continue;
@@ -378,6 +401,9 @@ export function createStreamer(basedir, tag) {
                             controller.enqueue(Uint8Array.from(chunk.chunk));
                         }
                     }
+                    if (readerCancelled) {
+                        return;
+                    }
                     // Finished reading from disk - now deliver buffered event chunks in chronological order
                     isReadingFromDisk = false;
                     // Sort buffered chunks by ULID (chronological order)
@@ -422,6 +448,9 @@ export function createStreamer(basedir, tag) {
                     // separate process (e.g. e2e test runner ↔ workbench app), polling
                     // the shared filesystem is the fallback delivery mechanism.
                     let isPolling = false;
+                    if (readerCancelled) {
+                        return;
+                    }
                     pollInterval = setInterval(async () => {
                         if (isPolling)
                             return;
@@ -476,7 +505,8 @@ export function createStreamer(basedir, tag) {
                     }, 100);
                 },
                 cancel() {
-                    removeListeners();
+                    readerCancelled = true;
+                    teardown();
                     if (pollInterval) {
                         clearInterval(pollInterval);
                         pollInterval = null;
