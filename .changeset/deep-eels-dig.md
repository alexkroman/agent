---
"@alexkroman1/aai": minor
---

The default AssemblyAI pipeline now sends `reasoningEffort: "none"` — on a voice line, time-to-first-token is the quality, and nothing downstream can cover the wait before the first token.
