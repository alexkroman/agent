---
"@alexkroman1/aai": patch
---

Keep one guest sandbox per slug and per studio project across web-service replicas: a cross-replica registry lets a cold broker route to a live peer's guest instead of spawning a duplicate.
