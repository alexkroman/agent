---
---

Verify studio session tokens locally with `getClaims` (Supabase's recommended
path) on the request path, keeping a server-side check on the account routes
where revocation matters. Private package (aai-server) only.
