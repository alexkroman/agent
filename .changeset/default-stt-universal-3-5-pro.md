---
"@alexkroman1/aai": minor
---

Default the AssemblyAI streaming STT model to `universal-3-5-pro` (Universal-3.5 Pro Real-Time) instead of `u3pro-rt`. The legacy `u3pro-rt` alias is still accepted and maps to the SDK's `u3-rt-pro`. (The `assemblyai` SDK is already on `^4.36.3`.)
