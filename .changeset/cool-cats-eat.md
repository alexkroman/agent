---
"@alexkroman1/aai": minor
---

Report upload progress from the workflow API client and render it with the new <UploadProgressBar>, so a form storing a large file shows a bar instead of a disabled button.
