---
"@alexkroman1/aai": patch
---

Model the pipeline turn state, the S2S connection lifecycle, the outward speaking-edge gate, and the browser session's agent state as XState statecharts, replacing the boolean latches whose illegal combinations were reachable.
