---
"aai-server": patch
---

Guest harness cleanup: one JSON responder, one glob matcher, one child-env picker; drop the TOOL_TIMEOUT_MS mirror and dead turn-budget/connect state; parallelize template reads and dedupe export hints.
