---
"@alexkroman1/aai-runtime": major
---

Remove the Workflow DevKit's world, compiled surface and queue callbacks. `AgentServerOptions` loses `workflowCode`/`stepCode` and the `SweepSkip` type is gone; neither has a mechanism behind it now.
