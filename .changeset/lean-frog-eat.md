---
"@alexkroman1/aai": minor
---

A throwing tool now emits a non-fatal `tool` error frame; `aai dev` typechecks in the background; and the workflow-app compile-error messages no longer appear in a voice agent's diagnostics.
