---
"@alexkroman1/aai": patch
---

Never silently fall back to S2S: forward pipeline providers based on the descriptors rather than the optional mode field, and log the resolved session mode once per runtime
