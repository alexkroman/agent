---
"aai-studio-server": patch
---

Keep @alexkroman1/aai-ui external to the service bundle so the default client UI resolves — deployed agent pages 500'd with "Could not locate the default client UI"
