---
"@alexkroman1/aai": major
---

Remove the workflow() app kind, the sync-turn API (POST /sync, createSyncSession, createPttRecorder, the WebSocket audio-file upload path), text-only mode (tts: none(), audioOut), and the Slack send channel (send:, @alexkroman1/aai/send, the send_message builtin).
