---
"@alexkroman1/aai": minor
---

Add `gateway()` LLM provider factory routing through the Vercel AI Gateway, so pipeline agents can use any `"creator/model"` id (e.g. `zai/glm-4.6`) with a single `AI_GATEWAY_API_KEY`.
