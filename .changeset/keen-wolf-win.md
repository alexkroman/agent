---
"@alexkroman1/aai": minor
---

Add the @alexkroman1/aai/workspace-files subpath: the walk, caps, skip rules and strict UTF-8 decode that define a studio workspace on disk. The CLI's push, the studio guest's end-of-turn sync and the platform's validation now read one definition instead of three copies that had to agree.
