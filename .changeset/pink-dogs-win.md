---
"@alexkroman1/aai-cli": minor
---

`aai init` now shows a template selector when `--template` is omitted, listing every template the CLI ships with `simple` pre-selected. `--yes` and JSON mode still take the default without prompting.
