---
---

Studio: fold Settings into the Preview/Code pane switcher and lay it out as a
full page (secrets, the CLI round-trip, and Delete project as section cards),
replacing the floating dropdown. The "Work locally" CLI commands no longer
carry `--server` — the CLI already targets the platform they were copied from.

Private package only (`aai-studio-client`), so nothing published changes.
