---
"@alexkroman1/aai": patch
---

The workflow-upload refusal now names both ways to switch a database on — `aai storage enable` and Settings → Database in the studio — and says that provisioning rebuilds the running agent, so redeploying is not the remedy.
