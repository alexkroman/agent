---
"@alexkroman1/aai": minor
---

Export the HTTP methods the guest's proxied routes answer (WORKFLOW_API_METHODS, CLIENT_CONFIG_METHODS), so the platform's route declaration is checked against the guest's own dispatch rather than written from memory.
