---
"@alexkroman1/aai-cli": patch
---

Print the visible project list when `aai pull` finds no such studio project, so a login scoped to the wrong account names itself instead of looking like a missing project.
