---
"@alexkroman1/aai-cli": patch
---

Simplify the three workflow templates: research-desk types its run snapshot with WorkflowOutputOf, transcription-desk drops two single-use result types and a hand-rolled hash, and every file step now agrees on one shape.
