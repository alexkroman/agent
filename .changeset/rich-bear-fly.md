---
"@alexkroman1/aai": minor
---

Workflow default UI is now a pure execution surface: sync turn responses carry the turn's tool calls (SyncTurnResponse.toolCalls), and WorkflowView renders the transcript plus those tool calls with no greeting and no assistant messages.
