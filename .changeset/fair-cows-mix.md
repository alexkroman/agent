---
"aai-server": patch
---

Default the studio coding agent's pipeline LLM to qwen3-next-80b-a3b on the AssemblyAI LLM Gateway (studio prompt, chat model list, and codegen evals); pipeline mode remains the default over the S2S voice agent API, and the evals now assert the default gateway model id
