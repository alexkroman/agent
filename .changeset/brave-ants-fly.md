---
"@alexkroman1/aai-runtime": patch
---

The local-storage boot announcement tells the truth in both compositions it is reachable from: under `aai dev` a run and its upload survive a restart, under a per-process data directory they do not.
