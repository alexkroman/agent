---
"@alexkroman1/aai-cli": minor
---

Add `aai build --target modal` for Modal deploys: emits a self-contained `.aai/modal/` directory plus a generated `app.py` that runs the bundled server behind `@modal.web_server`
