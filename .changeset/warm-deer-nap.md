---
"@alexkroman1/aai": minor
---

Rewrite the default system prompt as non-overlapping sections, spend the step after the tool budget on a forced final answer (so a capped turn answers with what it has instead of going silent mid-chain), and report each provider stage's effective settings at startup
