---
"@alexkroman1/aai": minor
---

Add snapshotSessionNotes/restoreSessionNotes to the runtime for cross-replica session resume: the platform server can now persist a disconnected session's remember/recall notes and restore them when the session resumes on another host.
