---
---

Test-only: `service-config.test.ts` now identifies the two platform pools by
`max` rather than by construction order, which its own note asked for once
`ADMIN_POOL_MAX` moved off 4. No package changes, so nothing to release.
