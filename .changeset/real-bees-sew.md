---
"@alexkroman1/aai": patch
---

Fix deployed agents silently losing the default cognitive builtins, remove dead KV/Vector HTTP handlers and wrong-vendor API-key fallbacks, and reuse shared helpers for PCM views, JSON parsing and error formatting.
