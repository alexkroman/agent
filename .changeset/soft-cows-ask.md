---
"@alexkroman1/aai-cli": patch
---

Document StartScreen as a wrapper component in the scaffold authoring guide: it requires `children`, and writing it self-closing is a TS2741 build error. Adds a minimal client.tsx example alongside the component table.
