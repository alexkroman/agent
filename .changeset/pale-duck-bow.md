---
"@alexkroman1/aai": minor
---

Generate the AssemblyAI gateway model catalog from the gateway's /v1/models endpoint, with per-model tools/stream/EU/liveness flags, and type assemblyAI({ model }) against it.
