---
"aai-studio-server": patch
---

Studio: drop the "this preview updates automatically as you edit" banner from the Preview pane. It rendered on every unpublished project — i.e. nearly always — restating the pane's own name above a Publish control already in the top bar. The failed-build banner is unchanged.
