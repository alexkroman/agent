---
"@alexkroman1/aai": minor
---

Add AssemblyAI LLM Gateway provider for pipeline mode: assemblyAI({ model, region? }) in @alexkroman1/aai/llm routes the LLM loop through the OpenAI-compatible gateway (25+ models) using ASSEMBLYAI_API_KEY
