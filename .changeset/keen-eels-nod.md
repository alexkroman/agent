---
"aai-studio-client": patch
---

Lock the Publish button while a chat turn streams — Publish now unlocks on the same end-of-turn event the preview deploy builds on, so a mid-turn workspace checkpoint can never be shipped to production.
