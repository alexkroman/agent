---
---

Developer-experience gates only — no published package changes. Line-cap and
CLAUDE.md headroom reporting, coverage floors measured by the local check the
way CI measures them, a reachable real-Postgres tier whose skip is loud, and a
declared guest-route exposure table the orchestrator's routes are checked
against.
