---
"@alexkroman1/aai-cli": major
---

Delete the DevKit's build pipeline. `aai build` no longer compiles a `workflows/` directory or embeds `__aaiWorkflowCode`/`__aaiStepCode`, and `workflow` leaves the CLI and the scaffold.
