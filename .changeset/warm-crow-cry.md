---
"@alexkroman1/aai": patch
"aai-templates": patch
---

Call the project the AssemblyAI Agent SDK in the READMEs, the documentation site, the shipped authoring guide, and the shipped skill. The npm package names, the `aai` CLI command, and every env var are unchanged.
