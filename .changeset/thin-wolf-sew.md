---
"@alexkroman1/aai": minor
---

Add workflow apps: agent({ page: "static" }), the workflow HTTP API (/workflows/*), page()/createWorkflowApi()/useWorkflowRun() in the browser client, and `aai workflow` for reading and steering runs from a terminal.
