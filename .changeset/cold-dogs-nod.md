---
"@alexkroman1/aai": minor
---

Publish which windows of an unfinished parts upload have landed (UploadInfo.ranges), and add UploadOptions.resume so a dropped upload finishes rather than starting over. useWorkflowStream resumes once before cancelling the run.
