{
  "agent": {
    "current": 9,
    "supported": [9],
    "dropped": {
      "1": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example.",
      "2": "Pre-release: `error.reported.fatal` and `ClientConfigResponse.page` are required now, and the deprecated `mapInBatches` alias is gone — a caller written against the older spelling no longer compiles, and no external consumer is owed it.",
      "3": "Pre-release: StandardSchemaIssue gains an OPTIONAL `errors` field (a vendor extension formatSchemaIssues reads for union branches), so this epoch would in fact still compile — dropped rather than retained because the SDK has no external consumers and contracts/compatibility/ stays empty.",
      "4": "ctx.db is removed from ToolContext: the platform provisions no database and hands tool code none, so an agent, tool, dialog or subagent body that read ctx.db no longer compiles.",
      "5": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example.",
      "6": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example.",
      "7": "`agent({ system })` is gone; the field is `systemPrompt`. The alias existed to match the Vercel AI SDK's spelling and made `agent({ system, systemPrompt })` representable, which then needed a runtime throw to forbid — one concept with two names and a rule to police them. A stale `system` is reported by name, because it is in the stray-field renames map.",
      "8": "Pre-release: `StaticAgentParams` is now `Omit<StaticAgentParamsCore, WorkflowAppOnlyField>` plus the misuse messages, rather than sharing a third unexported base with the arm — so this epoch would in fact still compile, the shape being identical. Dropped rather than retained because the SDK has no external consumers and contracts/compatibility/ stays empty. The change exists so the docs build has one name to resolve instead of two: TypeDoc refuses an undocumented type on a public signature, and `StaticAgentParamsBase` was a second one."
    }
  },
  "builtins": {
    "current": 2,
    "supported": [2],
    "dropped": {
      "1": "`webSearch` takes ONE name for its cap, in ONE position: `maxResults`, in the options bag. It used to accept `max_results` too — the only snake_case identifier on this SDK's TypeScript surface — and `maxResults` again in the second parameter, so one option had three ways to arrive and two were undiscoverable from the third. `max_results` remains the WIRE name the model-facing builtin schema declares; only the TypeScript helper narrowed."
    }
  },
  "channels": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "defaults": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "dialog": {
    "current": 6,
    "supported": [6],
    "dropped": {
      "1": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example.",
      "2": "Pre-release: StandardSchemaIssue gains an OPTIONAL `errors` field (a vendor extension formatSchemaIssues reads for union branches), so this epoch would in fact still compile — dropped rather than retained because the SDK has no external consumers and contracts/compatibility/ stays empty.",
      "3": "ctx.db is removed from ToolContext: the platform provisions no database and hands tool code none, so an agent, tool, dialog or subagent body that read ctx.db no longer compiles.",
      "4": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example.",
      "5": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example."
    }
  },
  "ffmpeg": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "generate": {
    "current": 2,
    "supported": [2],
    "dropped": {
      "1": "Pre-release: StandardSchemaIssue gains an OPTIONAL `errors` field (a vendor extension formatSchemaIssues reads for union branches), so this epoch would in fact still compile — dropped rather than retained because the SDK has no external consumers and contracts/compatibility/ stays empty."
    }
  },
  "llm": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "procedure": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "s2s": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "spoken": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "state": {
    "current": 6,
    "supported": [6],
    "dropped": {
      "1": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example.",
      "2": "Pre-release: StandardSchemaIssue gains an OPTIONAL `errors` field (a vendor extension formatSchemaIssues reads for union branches), so this epoch would in fact still compile — dropped rather than retained because the SDK has no external consumers and contracts/compatibility/ stays empty.",
      "3": "ctx.db is removed from ToolContext: the platform provisions no database and hands tool code none, so an agent, tool, dialog or subagent body that read ctx.db no longer compiles.",
      "4": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example.",
      "5": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example."
    }
  },
  "step": {
    "current": 3,
    "supported": [3],
    "dropped": {
      "1": "Pre-release: `error.reported.fatal` and `ClientConfigResponse.page` are required now, and the deprecated `mapInBatches` alias is gone — a caller written against the older spelling no longer compiles, and no external consumer is owed it.",
      "2": "Pre-release: StandardSchemaIssue gains an OPTIONAL `errors` field (a vendor extension formatSchemaIssues reads for union branches), so this epoch would in fact still compile — dropped rather than retained because the SDK has no external consumers and contracts/compatibility/ stays empty."
    }
  },
  "step-errors": {
    "current": 2,
    "supported": [2],
    "dropped": {
      "1": "Pre-release: StandardSchemaIssue gains an OPTIONAL `errors` field (a vendor extension formatSchemaIssues reads for union branches), so this epoch would in fact still compile — dropped rather than retained because the SDK has no external consumers and contracts/compatibility/ stays empty."
    }
  },
  "step-files": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "stt": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "subagent": {
    "current": 5,
    "supported": [5],
    "dropped": {
      "1": "Pre-release: StandardSchemaIssue gains an OPTIONAL `errors` field (a vendor extension formatSchemaIssues reads for union branches), so this epoch would in fact still compile — dropped rather than retained because the SDK has no external consumers and contracts/compatibility/ stays empty.",
      "2": "ctx.db is removed from ToolContext: the platform provisions no database and hands tool code none, so an agent, tool, dialog or subagent body that read ctx.db no longer compiles.",
      "3": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example.",
      "4": "`SubagentDef.instructions` is `systemPrompt`. It was the only field renamed between `subagent()` and its larger sibling — otherwise a field-for-field smaller `agent()` — so one concept had two names depending on which declaration you were writing. Migration is the rename at each `subagent({ … })` call site; a stale one is reported BY NAME, because `instructions` is in the stray-field renames map."
    }
  },
  "testing": {
    "current": 10,
    "supported": [10],
    "dropped": {
      "1": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example.",
      "2": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example.",
      "3": "Pre-release: `error.reported.fatal` and `ClientConfigResponse.page` are required now, and the deprecated `mapInBatches` alias is gone — a caller written against the older spelling no longer compiles, and no external consumer is owed it.",
      "4": "Pre-release: StandardSchemaIssue gains an OPTIONAL `errors` field (a vendor extension formatSchemaIssues reads for union branches), so this epoch would in fact still compile — dropped rather than retained because the SDK has no external consumers and contracts/compatibility/ stays empty.",
      "5": "createUnusedDb is removed with ctx.db: createToolContext no longer supplies a db, so a spec that imported the rejecting Db it defaulted to no longer compiles.",
      "6": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example.",
      "7": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example.",
      "8": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example.",
      "9": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example. `withDiscoveredTools` is no longer exported: it was the tools-only half of `deployedAgent`, and with `virtual:aai/agent` serving the whole lowering there is nothing left for a spec to reach it for."
    }
  },
  "tool": {
    "current": 6,
    "supported": [6],
    "dropped": {
      "1": "Pre-release: StandardSchemaIssue gains an OPTIONAL `errors` field (a vendor extension formatSchemaIssues reads for union branches), so this epoch would in fact still compile — dropped rather than retained because the SDK has no external consumers and contracts/compatibility/ stays empty.",
      "2": "ctx.db is removed from ToolContext: the platform provisions no database and hands tool code none, so an agent, tool, dialog or subagent body that read ctx.db no longer compiles.",
      "3": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example.",
      "4": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example.",
      "5": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example."
    }
  },
  "transcribe": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "tts": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "uploads": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "utils": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "workflow": {
    "current": 2,
    "supported": [2],
    "dropped": {
      "1": "Pre-release: StandardSchemaIssue gains an OPTIONAL `errors` field (a vendor extension formatSchemaIssues reads for union branches), so this epoch would in fact still compile — dropped rather than retained because the SDK has no external consumers and contracts/compatibility/ stays empty."
    }
  },
  "workflow-api": {
    "current": 3,
    "supported": [3],
    "dropped": {
      "1": "Pre-release: `error.reported.fatal` and `ClientConfigResponse.page` are required now, and the deprecated `mapInBatches` alias is gone — a caller written against the older spelling no longer compiles, and no external consumer is owed it.",
      "2": "Pre-release: StandardSchemaIssue gains an OPTIONAL `errors` field (a vendor extension formatSchemaIssues reads for union branches), so this epoch would in fact still compile — dropped rather than retained because the SDK has no external consumers and contracts/compatibility/ stays empty."
    }
  }
}
