{
  "client": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "client-dir": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "components": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "forms": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "hooks": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "page": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "session": {
    "current": 2,
    "supported": [2],
    "dropped": {
      "1": "Pre-release: TRANSCRIBING_PLACEHOLDER moved to @alexkroman1/aai-ui/internal. It is a tuning constant referenced by no public signature and named by no file outside this package, so it sat in a client.tsx author's autocomplete beside useSession while being a framework decision with no field to set."
    }
  },
  "theme": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "workflow": {
    "current": 5,
    "supported": [5],
    "dropped": {
      "1": "Pre-release: DEFAULT_PROGRESS_POLL_MS, DEFAULT_WORKFLOW_POLL_MS and MAX_MISSING_READS moved to @alexkroman1/aai-ui/internal. All three are tuning numbers referenced by no public signature and named by no file outside this package; the hooks that own them take the interval as an OPTION, which is the authoring surface for the same choice and stays public.",
      "2": "The workflow hooks' type parameter is the workflow DEFINITION, not its output type. Epoch 2's `useWorkflowSubmit<Digest>(\"digest\")` no longer compiles: the parameter is constrained to `AnyWorkflowDef`, deliberately, so passing an output type is a compile error rather than a silent loss of typing. What it buys is the other half of the contract — `submit(input)` was `unknown`, so `submit({ ur1: 42 })` compiled and arrived as a 400 in the browser while the same statement already derived the OUTPUT from the def. Migration is one token per call site: `<Digest>` becomes `<typeof digest>`, and the `WorkflowOutputOf` alias above it is deleted.",
      "3": "The workflow hooks' def type parameter is REQUIRED — there is no `unknown` fallback for an un-parameterized call. It existed so `useWorkflowSubmit(\"digest\")` kept compiling, and it was the last way to get an untyped `submit` back. A workflow that declares no input schema now gets `submit(): void` rather than an unusable `never` parameter.",
      "4": "Pre-release: the SDK has no external consumers, so no superseded authoring style is owed a compiling example. A workflow that declares no input schema gets `submit(undefined)` rather than `submit()`: `void` would let the argument be omitted, and Biome's `noConfusingVoidType` rejects it outside a return or type-parameter position."
    }
  }
}
