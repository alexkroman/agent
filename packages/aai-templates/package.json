{
  "name": "aai-templates",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "check:typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "aai-cli": "workspace:*",
    "aai-ui": "workspace:*",
    "@types/react": "^19.0.0",
    "aai": "workspace:*",
    "zod": "^4.3.6"
  }
}
