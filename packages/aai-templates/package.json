{
  "name": "aai-templates",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "check:typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "aai": "workspace:*",
    "aai-cli": "workspace:*",
    "aai-ui": "workspace:*",
    "zod": "^4.3.6"
  }
}
