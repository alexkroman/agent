// Copyright 2025 the AAI authors. MIT license.
/**
 * Frozen authoring example: `aai-runtime:runtime` epoch 4.
 *
 * Replace this scaffold with a representative example of how the aai-runtime:runtime
 * capability was authored at epoch 4. It must keep compiling against
 * current source for as long as that epoch is advertised as supported —
 * REPLACE_WITH_A_REAL_AUTHORING_EXAMPLE
 */

export {};
