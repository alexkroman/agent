{
  "db": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "eval": {
    "current": 7,
    "supported": [1, 2, 3, 4, 5, 7],
    "dropped": {
      "6": "SessionEvent and SessionEventBody left @alexkroman1/aai/protocol for the root barrel, where the new aai:events capability owns them (on /protocol they were owned by nothing and hashed in full by every capability that reached them). This epoch rollup imports them from /protocol, so it no longer compiles, and neither does a host that wrote against this surface importing either name from there: import them from @alexkroman1/aai. Nothing about this capability own signatures changed."
    }
  },
  "keys": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "logging": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "providers": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "runtime": {
    "current": 4,
    "supported": [1, 2, 3, 4],
    "dropped": {}
  },
  "server": {
    "current": 4,
    "supported": [1, 2, 3, 4],
    "dropped": {}
  },
  "session": {
    "current": 4,
    "supported": [1, 2, 4],
    "dropped": {
      "3": "SessionEvent and SessionEventBody left @alexkroman1/aai/protocol for the root barrel, where the new aai:events capability owns them (on /protocol they were owned by nothing and hashed in full by every capability that reached them). This epoch rollup imports them from /protocol, so it no longer compiles, and neither does a host that wrote against this surface importing either name from there: import them from @alexkroman1/aai. Nothing about this capability own signatures changed."
    }
  },
  "session-state": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "telephony": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "testing": {
    "current": 3,
    "supported": [1, 3],
    "dropped": {
      "2": "SessionEvent and SessionEventBody left @alexkroman1/aai/protocol for the root barrel, where the new aai:events capability owns them (on /protocol they were owned by nothing and hashed in full by every capability that reached them). This epoch rollup imports them from /protocol, so it no longer compiles, and neither does a host that wrote against this surface importing either name from there: import them from @alexkroman1/aai. Nothing about this capability own signatures changed."
    }
  },
  "text": {
    "current": 3,
    "supported": [1, 3],
    "dropped": {
      "2": "SessionEvent and SessionEventBody left @alexkroman1/aai/protocol for the root barrel, where the new aai:events capability owns them (on /protocol they were owned by nothing and hashed in full by every capability that reached them). This epoch rollup imports them from /protocol, so it no longer compiles, and neither does a host that wrote against this surface importing either name from there: import them from @alexkroman1/aai. Nothing about this capability own signatures changed."
    }
  },
  "tools": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "tracing": {
    "current": 2,
    "supported": [1, 2],
    "dropped": {}
  },
  "uploads": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  },
  "workflow": {
    "current": 1,
    "supported": [1],
    "dropped": {}
  }
}
